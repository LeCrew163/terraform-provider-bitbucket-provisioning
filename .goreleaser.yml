version: 2

project_name: terraform-provider-bitbucket-provisioning

before:
  hooks:
    - go mod tidy

builds:
  - env:
      - CGO_ENABLED=0
    mod_timestamp: '{{ .CommitTimestamp }}'
    flags:
      - -trimpath
    ldflags:
      - '-s -w -X main.version={{.Version}}'
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
      - arm
    goarm:
      - '7'
    ignore:
      - goos: darwin
        goarch: arm
    binary: '{{ .ProjectName }}_v{{ .Version }}'

archives:
  - format: zip
    name_template: '{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}'
    files:
      - LICENSE
      - README.md
      - CHANGELOG.md

checksum:
  name_template: '{{ .ProjectName }}_{{ .Version }}_SHA256SUMS'
  algorithm: sha256

# No GitHub/GitLab release — distributed via Artifactory Terraform registry
release:
  disable: true

changelog:
  use: git
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'
      - typo
  groups:
    - title: 'Features'
      regexp: '^.*?feat(\([[:word:]]+\))??!?:.+$'
      order: 0
    - title: 'Bug Fixes'
      regexp: '^.*?fix(\([[:word:]]+\))??!?:.+$'
      order: 1
    - title: 'Others'
      order: 999

# Upload zip archives + checksums to Artifactory.
# Path mirrors the registry layout: terraform/<namespace>/<type>/<version>/
# The manifest.json is generated and uploaded separately in the Jenkins pipeline.
artifactories:
  - name: internal
    target: 'http://art01.sldnet.de:8081/artifactory/terraform/swisslife/bitbucket-provisioning/{{ .Version }}/'
    username: '{{ .Env.ARTIFACTORY_USERNAME }}'
    secret: '{{ .Env.ARTIFACTORY_SECRET }}'
