# OpenAPI Generator Ignore
# Generated by openapi-generator https://github.com/openapitools/openapi-generator

# Use this file to prevent files from being overwritten by the generator.
# The patterns follow closely to .gitignore or .dockerignore.

# As an example, the C# client generator defines ApiClient.cs.
# You can make changes and tell OpenAPI Generator to ignore just this file by uncommenting the following line:
#ApiClient.cs

# You can match any string of characters against a directory, file or extension with a single asterisk (*):
#foo/*/qux
# The above matches foo/bar/qux and foo/baz/qux, but not foo/bar/baz/qux

# You can recursively match patterns against a directory, file or extension with a double asterisk (**):
#foo/**/qux
# This matches foo/bar/qux, foo/baz/qux, and foo/bar/baz/qux

# You can also negate patterns with an exclamation (!).
# For example, you can ignore all files in a docs folder with the file extension .md:
#docs/*.md
# Then explicitly reverse the ignore rule for a single file:
#!docs/README.md

# The generated package is part of the main module — do not overwrite these
# files which are owned by the parent project, not the generator.
go.mod
go.sum
README.md
git_push.sh
.travis.yml

# api_deprecated.go declares duplicate types that conflict with the regular API
# files (e.g. ApiApproveRequest, ApiGetBuildStatusStatsRequest). The deprecated
# endpoints are not used by this provider, so we exclude the file entirely.
# client.go is also excluded because it references DeprecatedAPIService from the
# excluded file — the committed version has those two lines removed.
api_deprecated.go
client.go

# These models have mutually recursive types. The generator emits value types
# ([]Comment, CommentThread) which the Go compiler rejects as infinite-size
# structs. The committed versions use pointer types (*Comment, *CommentThread)
# which are correct. None of these models are used by the provider.
model_comment.go
model_comment_thread.go
model_pull_request_participant.go

# Stub tests generated by openapi-generator — they only contain boilerplate
# and import the module by its full path, which would need updating on every
# module rename. The provider has its own tests under internal/provider/.
test/
